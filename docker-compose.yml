services:
  web:
    build: .
    ports:
      - "5173:5173"
    volumes:
      # Mount your code in so edits show up live...
      - .:/app
      # ...but keep the container's own node_modules
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
